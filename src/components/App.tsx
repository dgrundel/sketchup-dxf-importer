import * as React from 'react';
import { Text } from '@fluentui/react';

export const App = () => {
    return <div>
        <Text variant={'mega'} block>
            Hello, React!
        </Text>
    </div>;
};